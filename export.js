'use strict';window.FFHExport={csv(name,rows){if(!rows.length)return alert('No data to export');let keys=Object.keys(rows[0]),q=v=>'"'+String(v??'').replaceAll('"','""')+'"',csv=[keys.map(q).join(','),...rows.map(r=>keys.map(k=>q(r[k])).join(','))].join('\n'),a=document.createElement('a');a.href=URL.createObjectURL(new Blob([csv],{type:'text/csv'}));a.download=name+'.csv';a.click();URL.revokeObjectURL(a.href)}};